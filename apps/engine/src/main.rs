//! electric-lite engine binary. The control-plane HTTP server + circuit actors land in M1;
//! for the spike this is just a placeholder so the `[[bin]]` target resolves.

fn main() {
    println!("electric-lite-engine: placeholder (server implemented in M1)");
}
