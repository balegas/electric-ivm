[workspace]
members = ["apps/engine"]
resolver = "2"

[workspace.package]
edition = "2024"
version = "0.0.0"
license = "MIT OR Apache-2.0"

[profile.dev]
# dbsp is a heavy dependency; keep dev builds as fast as we can.
opt-level = 0
debug = 1
